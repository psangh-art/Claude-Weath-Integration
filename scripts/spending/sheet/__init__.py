"""The Wealth Summary workbook build, one module per phase (2026-07-19).

write_excel() in spending_summary.py sets the context up and calls these in
order: assets -> spending -> income -> totals -> targets -> finish.
"""
